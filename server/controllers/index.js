const authController = require('./auth');
const furnitureController = require('./furnitureController');

const transportController = require('./transportControler');

module.exports = {
    authController,
    furnitureController,
    
    transportController,
}